/**
 * @deprecated Import from `@/shared/form/field-input.util` (or `@/shared/form`).
 * Kept so existing call sites keep working.
 */
export { capitalizeFirstLetter } from "@/shared/form/field-input.util";
